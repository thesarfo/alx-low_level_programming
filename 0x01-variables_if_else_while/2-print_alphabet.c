#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 * followed by a new line
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char letter;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(letter);
	}
	putchar('\n');
	return (0);
}
